import React from 'react';
import { shallow } from 'enzyme';
import SearchBar from './search_bar';

describe('<SearchBar />', () => {
    test('should be able to type in a city', () => {
        
    });
});